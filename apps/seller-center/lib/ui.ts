import type { CSSProperties } from 'react';

/** Estilos mínimos do Seller Center (a UI real vem com `packages/ui`). */
export const pagina: CSSProperties = {
  fontFamily: 'system-ui, sans-serif',
  padding: '2rem',
  maxWidth: '60rem',
  margin: '0 auto',
  lineHeight: 1.5,
};

export const alerta: CSSProperties = {
  padding: '.75rem',
  border: '1px solid #f0c0c0',
  background: '#fff5f5',
  borderRadius: '.375rem',
};
